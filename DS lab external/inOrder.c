#include<stdio.h>

struct Node{
    int data;
    struct Node* left;
    struct Node* right;
};
typedef struct Node Node;


Node* root = NULL;
void inOrder(Node* root)
{
    
}

int main()
{
    return 0;
}